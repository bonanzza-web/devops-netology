#cloud-config
users:
  - name: ubuntu
    groups: sudo
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    package_update: true
    package_upgrade: false
    packages:
      - vim
      - nginx
    ssh_authorized_keys:
      - ${ssh_key}